encoder:
  latent_dim: 192
  transformer:
    num_layers: 3
    num_heads: 4
    mlp_ratio: 4.0

predictor:
  latent_dim: 192
  action_embed_dim: 16
  hidden_dim: 256

loss:
  num_subspaces: 16
  subspace_dim: null
  num_slices: 16
  reg_weight: 1.0
