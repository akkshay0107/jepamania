encoder:
  latent_dim: 192
  transformer:
    num_layers: 3
    num_heads: 4
    mlp_ratio: 4.0

predictor:
  latent_dim: 192
  action_embed_dim: 16
  hidden_dim: 256

planner:
  type: "beam"
  sequence_len: 10
  smoothness_weight: 0.0
  beam_width: 6
