[project]
name = "win-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "core",
    "h5py",
    "tmrl; sys_platform == 'win32'",
]

[tool.uv.sources]
core = { workspace = true }
