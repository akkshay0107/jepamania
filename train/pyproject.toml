[project]
name = "train"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "core",
    "h5py",
    "jax[cuda12]; sys_platform == 'linux'",
    "wandb",
]

[tool.uv.sources]
core = { workspace = true }
